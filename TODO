#import utilities from a csv

#solicit type probabilities

#perturb the utilities with some epsilon

#iterate through possible assignments (probabilistic)
	#output the utility from each
	#store utility in some array

#find max of the array
	#if array too large, use stochastic gradient descent



